keep users signed in for a day; fix owner profile info + photo flow

Session persistence
- store a loginAt timestamp on login; add getActiveSession() / clearSession()
  helpers with a 24h TTL in api/services/auth
- SplashScreen restores a valid session and routes straight to the role's home
  screen; only shows Login when there is no session or it has expired
- login resets the nav stack to the home route; logout goes through clearSession

Owner "Add Information" now actually saves
- send account_id (was running UPDATE ... WHERE account_id = NULL and faking
  success)
- pass gender as a string instead of the dropdown's setValue callback
- send birthdate as YYYY-MM-DD; navigate to the real RestaurantHome route
- backend: save the uploaded photo via default_storage, COALESCE the column,
  require account_id, return "Account not found" on 0 rows, and RETURNING the
  updated user row

Profile photo display
- add constants/config.js with mediaUrl() to build /media/ URLs from stored
  relative paths
- Restaurant/Diner profile screens render the avatar via mediaUrl() with a
  bundled fallback, and re-read the cached user on focus
- hook prefills the info screen from the cached user (edit mode), merges the
  returned user into AsyncStorage after saving, and only uploads a freshly
  picked local file

Network-error hardening
- addAdditionalInfo(): explicit multipart header, 20s timeout, clearer errors
- image picker keeps the full asset (uri/fileName/type) and handles cancel


