commits/

Draft commit messages, one per file in ../updates/ with the same name. Each
is written to be used as-is: a short summary line, then a body grouped by
area. The full context (testing, gotchas, follow-ups) is in the matching
updates/ file.
